// Create a new HTML element.
var div = document.createElement("div");

// Set the HTML element's content.
div.innerHTML = "Hello, world!";

// Append the HTML element to the body of the page.
document.body.appendChild(div);